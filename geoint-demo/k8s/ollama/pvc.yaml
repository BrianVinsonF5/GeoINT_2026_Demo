apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models-pvc
  namespace: geoint-demo
  labels: { app: ollama }
  annotations:
    nfs.io/storage-path: "ollama"
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: { storage: 10Gi }