apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: geoint-demo
  labels:
    app: geoint-frontend
spec:
  type: ClusterIP
  selector:
    app: geoint-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
