apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  namespace: geoint-demo
  labels:
    app: rag-api
spec:
  type: ClusterIP
  selector:
    app: rag-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
